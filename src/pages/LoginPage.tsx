import { Login } from '../features/auth/Login';

const LoginPage = () => {
  return (
    <div className="flex h-dvh items-center justify-center overflow-hidden bg-page px-3">
      <Login />
    </div>
  );
};

export default LoginPage;
